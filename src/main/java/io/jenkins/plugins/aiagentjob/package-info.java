/**
 * Core implementation for the AI Agent Job Jenkins plugin: job type, build step executor, event
 * parsing, usage accounting, and approval-gate coordination.
 */
package io.jenkins.plugins.aiagentjob;
